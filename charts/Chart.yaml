apiVersion: v2
name: leapmailr-ui
description: Helm chart for deploying the leapmailr-ui frontend workload (Deployment/Service/Ingress) driven by values.
type: application
version: 0.2.0
appVersion: "0.2.0"
